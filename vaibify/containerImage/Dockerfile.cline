# Vaibify — Cline CLI overlay

ARG BASE_IMAGE=vaibify:latest
FROM ${BASE_IMAGE}

SHELL ["/bin/bash", "-o", "pipefail", "-c"]

USER ${CONTAINER_USER}

RUN npm install --global --fetch-retries=3 cline \
    || { echo "vaibify build: Cline installation failed" >&2; exit 1; }
