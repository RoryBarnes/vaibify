# Vaibify — OpenHands CLI overlay

ARG BASE_IMAGE=vaibify:latest
FROM ${BASE_IMAGE}

SHELL ["/bin/bash", "-o", "pipefail", "-c"]

USER ${CONTAINER_USER}

RUN UV_HTTP_RETRIES=3 uv tool install openhands --python 3.12 \
    || { echo "vaibify build: OpenHands installation failed" >&2; exit 1; }
